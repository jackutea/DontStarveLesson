7.16 《饥荒》课程:
√ 1. 生成角色:
- 创建一个Prefab
- 用 UnityEngine.GameObject.Instantiate() 生成角色
√ 2. 移动角色
- 用 transform.position 设置角色的位置
√ 3. 相机跟随
√ 4. 生成资源（可以采的草）
√ 5. 角色采草

- 创建完一个类之后, 切到Unity里刷新一下

- 快捷键
ctrl + x 剪切一行
如果不选中的话, ctrl + c是复制一行, ctrl +v 是粘贴一行
ctrl + z 是 撤销
alt + shift + f 是格式化代码(自动排版)
F2 是重命名

- 预制体(prefab)
功能: 可以重复使用一个对象
预制体(预制件) -> 生成 实例(Instance)
生成方式有两种: 
1. 拖拽
2. 代码生成

- 但凡 Inspector 面板里有的属性, 代码里都可以修改

- List<T> 用法
Add
Remove
Clear
Find
for

- 伪代码， 用于疏理思路

- 让代码更好地阅读、复用 -> 称之为程序设计